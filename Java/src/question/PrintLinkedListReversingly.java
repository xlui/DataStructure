package question;

/**
 * 输入一个链表的头结点，从尾到头打印出每个结点的数值
 *
 * 第一种解法是利用栈，每经过一个结点，把该结点放到一个栈中。
 * 遍历完整个链表后，再从栈顶开始逐个输出结点的值。
 *
 * 第二种解法是递归，因为递归本质上也是一个栈结构。
 *
 * 如果可以改变链表的结构的话，还可以先将链表转置然后
 * 遍历输出
 */
public class PrintLinkedListReversingly {
}
